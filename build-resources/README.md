# Build Resources

Place platform-specific assets here:

- Windows: `icon.ico` (256x256) for installer & app.
- macOS (future): `icon.icns`.
- Linux (future): `icons/` with PNG sizes (16,32,48,64,128,256).

Currently using placeholder (no custom icon). Add assets before release branding.
